import User from "./User";
export type RoomUpdateType = {
    id?: string;
    title: string;
    desc: string;
    category: string;
    tags: string[];
    streams?: ArrayBuffer[];
    admin: User;
    users?: User[];
    limit: number;
    createdAt?: number;
    updatedAt?: number;
};
export default class Room {
    id: string;
    title: string;
    desc: string;
    category: string;
    tags: string[];
    streams: ArrayBuffer[];
    link: string;
    linkDesc: string;
    likes: string[];
    views: number;
    admin?: User;
    users: User[];
    limit: number;
    createdAt: number;
    updatedAt: number;
    constructor(id: string);
    setAdmin(user: User): void;
    setLink(link: string): void;
    setLinkDesc(linkDesc: string): void;
    setTitle(title: string): void;
    findUser(userId: string): User;
    hasUser(userId: string): boolean;
    join(user: User): void;
    out(userId: string): User;
    updateUser(userId: string, userData: User): User;
    updateRoom(roomData: Room): this;
    addStream(stream: string): void;
    getStream(index: number): string;
    isEmpty(): boolean;
    addLike(userId: string): boolean;
}
